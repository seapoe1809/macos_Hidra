"""Service layer: the working logic refactored out of the original scripts.

These modules contain no web/UI code — they are plain importable functions so
the FastAPI routers (and tests) can call them directly.
"""
